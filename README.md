# Create .env file with the same contents as .env.example, ensuring the missing secrets/API keys are filled in approriately.
# From project root directory run docker compose build then docker compose up, application will run on localhost:3000
